#include "push_swap.h"

// LEFTOFF!

// algo that calculates all the steps for each node, NO connecting nodes!
// just looking at the right place it should go in b.

// node->ind = amound of r, node->ind - total list_len = rr (so it will be negative).