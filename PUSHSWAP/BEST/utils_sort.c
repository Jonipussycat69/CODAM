#include "push_swap.h"

void	act_arr_reset(t_sort *s)
{
	short	i;

	i = 0;
	while (i < 8)
	{
		s->act_arr[i] = 0;
		i++;
	}
	return ;
}

void	weigh(t_list **a, t_list **b, t_sort *s, t_list *node)
{
	// LEFTOFF
}
