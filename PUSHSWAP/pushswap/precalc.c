#include "push_swap.h"

// Looks for the path combination with the best sortedness (in the other list)
int	path_combi(t_list **head_a, t_list **head_b, t_list *s)
{
	int		i;
	double	sortedness;
	double	best_sort;

	i = 0;
	while ()// LEFTOFF
	{
		
	}
	while ()
	{

	}
	while ()
	{

	}
	return (best_sort);
}

double	precalc(t_list **head_a, t_list **head_b, t_sort *s)
{
	double	weight;

	return (weight);
}