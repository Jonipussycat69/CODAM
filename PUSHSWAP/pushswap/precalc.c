#include "push_swap.h"

// Functions to calculate the state of teh list after given actions
// without actually performing them.

double	path_combi(t_list **head_a, t_list **head_b, t_list *s)
{
	int		i;
	double	sortedness;
	double	best_sort;

	i = 0;
	while ()// LEFTOFF maybe first init act_arr?
	{

	}
	while ()
	{

	}
	while ()
	{

	}
	return (best_sort);
}

double	precalc(t_list **head_a, t_list **head_b, t_sort *s)
{
	double	weight;

	return (weight);
}